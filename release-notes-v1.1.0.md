## 超语音 Windows v1.1.0

一次大更新：识别更准、操作更顺手、跑得更快、更安全、装起来更方便，一共加了 27 项功能。

### 识别更准
- 可以换成中/大号识别模型（设置页选，自动下载，不用重启）
- 专业词汇纠错表（自己维护"错词->正确词"，识别完自动替换）
- 中英混说识别（"帮我 commit 一下"这种中英夹杂的话识别准确率明显提升）
- 标点自动补全
- 口头命令（说"删除这段/换行/全部大写"直接执行）
- 按当前软件自动调整识别语气（微信偏口语，代码编辑器偏保留英文术语）

### 操作更顺手
- 录音悬浮条实时显示识别出的文字（不用干等）
- 设置页新增：模型选择、说话方式（按住/点击切换）、先出草稿再确认、专业词典编辑器、导入导出设置
- 按软件记住不同的专属快捷键
- 语音宏（一句话触发"打开软件+打字"等一串动作）
- 静音自动暂停

### 跑得快、不卡顿
- 显卡加速识别（有兼容显卡自动用，没有自动退回 CPU，快 10 倍以上）
- 说话时自动提升进程优先级，空闲时自动降低，不影响你打游戏/跑大任务
- 启动提速（发布产物内置 ReadyToRun 预编译，实测快 40-50%）
- 设置页新增内存/CPU占用面板

### 安全与隐私
- 全程离线的承诺写进了设置页
- 历史记录支持自动过期（7/30/90天可选）和一键清空
- 独立安全审计：密钥泄露、代码漏洞、依赖库已知漏洞全部为零

### 装起来、用起来更方便
- **真正的安装包**：双击 Setup.exe 即可安装，不用再解压文件夹
- **真正的一键更新**：托盘菜单检测到新版本后自动下载并重启到新版本
- 开机自启开关
- 新手引导（首次打开三步动画教学）
- 崩溃自动上报（脱敏，不含你说过的任何话）

---
本地识别，不联网、不上传录音或文字到任何服务器（下载更大识别模型时除外，会联网访问 Hugging Face）。
