# Aevocis Rust + Slint 候选功能与指标方案

> 本文件是产品选择清单，不代表本轮全部实现。当前轮次只修复已复现的 Rust/Slint 编译与窗口状态缺陷，并完成可交付的 Windows Rust+Slint 基线；带有“待选择”的项目，等用户选择后再进入实现范围。

## 选择规则

- 质量、可靠性、安全性、可维护性和扩展性优先；开发工作量不参与取舍。
- 指标必须能在真实用户路径中测量，不能用“感觉更快”“更美观”作为验收条件。
- P0 是影响核心听写、数据安全或安装更新的指标；P1 是明显提升效率和体验的指标；P2 是可插拔增强项。
- “默认基线”是本轮必须保持或修复的现有能力；“待选择”不在本轮擅自实现。

## 候选矩阵

<span id="M01"></span><span id="M02"></span><span id="M03"></span><span id="M04"></span><span id="M05"></span><span id="M06"></span><span id="M07"></span><span id="M08"></span><span id="M09"></span><span id="M10"></span><span id="M11"></span><span id="M12"></span><span id="M13"></span><span id="M14"></span><span id="M15"></span><span id="M16"></span><span id="M17"></span><span id="M18"></span><span id="M19"></span><span id="M20"></span><span id="M21"></span><span id="M22"></span><span id="M23"></span><span id="M24"></span><span id="M25"></span><span id="M26"></span><span id="M27"></span><span id="M28"></span><span id="M29"></span><span id="M30"></span><span id="M31"></span><span id="M32"></span><span id="M33"></span><span id="M34"></span><span id="M35"></span><span id="M36"></span><span id="M37"></span><span id="M38"></span><span id="M39"></span><span id="M40"></span><span id="M41"></span><span id="M42"></span><span id="M43"></span><span id="M44"></span>

| ID | 类别 | 候选功能/指标 | 可验证目标 | 核心算法或技术方案 | 优先级/状态 |
|---|---|---|---|---|---|
| M01 | 核心体验 | 热键首响应延迟 | 从真实按键到录音状态可见 P95 ≤ 80 ms | 低级键盘钩子只做状态切换；设备打开移到事件循环下一拍 | P0 默认基线 |
| M02 | 核心体验 | 松键到文字延迟 | 1–10 秒音频从松键到注入 P95 ≤ 1.0 s（不含模型首次加载） | 录音停止后把识别放到后台线程，UI 线程只做轻量后处理 | P0 默认基线 |
| M03 | 核心体验 | 长文本注入正确率 | 10,000 UTF-16 单元注入无错序、无截断；焦点切换时立即停止 | 分块 `SendInput` + 每块重新校验目标窗口 | P0 默认基线 |
| M04 | 核心体验 | 目标窗口安全 | 识别期间切换窗口时，目标窗口不得收到新文本 | `HWND + PID + IsWindow` 令牌校验；注入前及分块前复核 | P0 默认基线 |
| M05 | 核心体验 | 托盘单击显示/隐藏 | 单击托盘图标可反复切换主界面，隐藏时无任务栏按钮 | TrayIcon 左键事件 + Slint `hide()`；主窗关闭请求返回隐藏 | P0 默认基线 |
| M06 | 核心体验 | 托盘右键菜单有效性 | 显示、设置、更新、退出四项均触发可观察结果 | `MenuEvent` ID 路由；所有菜单项均绑定真实处理器 | P0 默认基线 |
| M07 | 核心体验 | 全局显示/隐藏快捷键 | 默认 `Ctrl+Alt+H`，重绑并保存后立即生效 | Win32 `RegisterHotKey` 隐藏消息窗；配置持久化 | P0 默认基线 |
| M08 | 核心体验 | 按住/切换模式 | Hold 与 Toggle 均能避免重复触发；Toggle 抖动窗口 ≥ 300 ms | 原始 VK 回调 + 状态机 + 去抖 | P0 默认基线 |
| M09 | 核心体验 | 快捷键重绑冲突提示 | 组合键被其他程序占用时不覆盖旧配置，托盘/设置有可见失败状态 | 注册失败保留旧注册；错误回传 UI | P1 待选择增强 |
| M10 | 音频 | 自动设备稳定性 | 设备拔插或蓝牙模式切换后，下一次录音不崩溃且有明确状态 | 设备枚举失败降级系统默认；失败路径恢复 Idle | P0 默认基线 |
| M11 | 音频 | 双设备自动选优 | 双路录音时选中语音能量 P90 更高的一路；双路 CPU 增量 < 2% | 20 ms 帧 RMS，使用 P90 抗恒定底噪与切换死区 | P1 待选择增强 |
| M12 | 音频 | 语音活动检测 | 静音不触发识别；误触发率 ≤ 1%（固定测试集） | 自适应噪声底 + 短时能量/零交叉率双阈值 | P1 待选择增强 |
| M13 | 音频 | 回声/扬声器抑制 | 扬声器播放时不把系统音频当成主要语音 | WASAPI 回声参考或设备回采 + NLMS 自适应滤波 | P2 待选择增强 |
| M14 | 识别 | SenseVoice 稳态延迟 | 6 秒中文音频稳态 P95 ≤ 300 ms | 常驻 `Arc<Recognizer>`、4 线程上限、一次性 CTC 解码 | P0 默认基线 |
| M15 | 识别 | 识别常驻内存 | 模型加载后工作集目标 ≤ 400 MB | int8 模型；单实例常驻，禁止每次听写重复加载 | P0 默认基线 |
| M16 | 识别 | 中英混合保真 | 中文、英文、数字混合句不丢英文大小写和数字 | `language=auto` + 领域词典后处理 | P0 默认基线 |
| M17 | 识别 | 领域词典命中率 | 词典中的错误词在目标边界内 100% 替换；超串不误替换 | CJK 子串替换；拉丁/数字使用字符边界扫描 | P0 默认基线 |
| M18 | 识别 | 句末标点一致性 | 中文文本缺标点补 `。`；英文补 `.`；已有标点不重复 | 非空文本 + CJK 字符占比 ≥ 50% 决定标点 | P0 默认基线 |
| M19 | 识别 | 非语音抑制 | `[BLANK_AUDIO]`、`(noise)` 等不注入、不进历史 | 对成对括号标记做早期丢弃 | P0 默认基线 |
| M20 | 识别 | 语音命令确定性 | “取消/换行”只匹配完整短语；“全部大写”只匹配后缀 | 去除尾部标点后，完整匹配/后缀匹配分离 | P0 默认基线 |
| M21 | 自动化 | 语音宏可恢复性 | 宏中一项失败时，后续独立动作仍执行并显示错误 | 每动作独立 `Result`；错误聚合返回 UI | P1 待选择增强 |
| M22 | 自动化 | 宏权限隔离 | 禁止任意 shell 拼接；只允许显式应用/URL/固定按键动作 | `ShellExecuteW` 参数分离；动作类型白名单 | P0 默认基线 |
| M23 | 数据 | 设置持久化成功率 | 进程重启后保存设置 100% 保留；旧 JSON 可继续读取 | 写临时文件后 Win32 `MoveFileExW(REPLACE_EXISTING)` 替换 | P0 默认基线 |
| M24 | 数据 | 词典取消语义 | 取消编辑后，未保存行不影响下一次听写 | 每次打开从磁盘加载；关闭即丢弃控制器 | P0 默认基线 |
| M25 | 数据 | 历史保留策略 | 7/30/90 天按 Unix 秒清理，未知时间戳不误删 | 精确字符串时间戳往返；按 cutoff 过滤 | P0 默认基线 |
| M26 | 数据 | 历史大小上限 | 历史最多保留 300 条，写盘后仍是合法 JSON | newest-first 截断 + 原子写入 | P0 默认基线 |
| M27 | 稳定性 | 单实例 | 多次启动只保留一个后台实例，不重复注册托盘/热键 | Win32 命名互斥体 + 前台唤醒 | P0 默认基线 |
| M28 | 稳定性 | 崩溃报告隐私 | 报告不包含历史、设置、录音、原始按键序列 | 仅记录 panic、位置、架构和回溯；最多 10 份轮转 | P0 默认基线 |
| M29 | 稳定性 | UI 无阻塞 | 更新下载、设备枚举、识别不阻塞 UI 线程 > 50 ms | 后台线程 + UI 线程消息回传 | P0 默认基线 |
| M30 | 性能 | 空闲资源 | 无录音/识别时 CPU P95 ≤ 1%，不持续轮询高耗能任务 | 低频资源计时器；更新检查单次后台执行 | P1 待选择增强 |
| M31 | 性能 | UI 绘制稳定性 | 主窗口与设置窗口滚动操作无明显卡顿，输入响应 < 100 ms | Slint 原生控件；列表模型增量更新；避免大文本同步 IO | P1 待选择增强 |
| M32 | 安全 | 发布密钥扫描 | 工作区和 Git 历史 gitleaks 0 命中 | 发布前全历史扫描，命中即阻断 | P0 默认基线 |
| M33 | 安全 | 代码安全扫描 | semgrep 可利用级 ERROR/WARNING 0 命中或有明确处置 | `p/security-audit` + 语言规则，关闭遥测 | P0 默认基线 |
| M34 | 安全 | 更新包完整性 | 下载后校验 GitHub asset SHA-256，不通过则不运行 | API `digest` + 本地哈希校验，失败保留旧版本 | P0 默认基线 |
| M35 | 安全 | 安装权限最小化 | 默认当前用户安装，不强制管理员；安装目录可选但叶目录恒为 `Aevocis` | Inno Setup `PrivilegesRequired=lowest` + 目录页归一化 | P0 默认基线 |
| M36 | 交付 | 安装包可复现 | 干净机器选择 `D:\Apps` 后实际路径是 `D:\Apps\Aevocis`，可卸载/重装 | Inno Setup 脚本化目录验证 + 模型资源清单 | P0 默认基线 |
| M37 | 交付 | Rust 版本线更新 | 新版本只被 `native-rust-v*` 更新器识别，不误装 C# 线 | GitHub release tag 前缀过滤 + 语义版本比较 | P0 默认基线 |
| M38 | 交付 | 发布资产可验证 | EXE、安装器、SHA-256 清单和版本说明一致 | 构建后 SHA-256 manifest；release 资产白名单 | P0 默认基线 |
| M39 | 跨平台 | Android/平板适配 | 同一 Rust 核心可在 Android 构建并显示 Slint UI；权限失败可解释 | 将 Win32 适配层抽为平台端口；Android 音频/输入采用平台实现 | P1 待选择架构 |
| M40 | 跨平台 | 响应式界面 | 电脑、手机、平板在横竖屏下无横向溢出，核心按钮首屏可见 | Slint 自适应布局 + 输入法/安全区适配 | P1 待选择架构 |
| M41 | 扩展 | 模型后端插件化 | 增加第二识别引擎无需改动 UI、历史、注入链 | `TranscriptionEngine` trait + 后端注册表 | P1 待选择架构 |
| M42 | 扩展 | Claude Code 调用 | Claude Code 可调用诊断、构建、打包命令并取得机器可读结果 | CLI 子命令 + JSON 输出 + 无敏感默认输出 | P1 待选择增强 |
| M43 | 可观测性 | 本地性能报告 | 用户可导出启动/录音/识别/注入耗时，不包含语音文本 | 环形计时样本 + 脱敏结构化日志 | P1 待选择增强 |
| M44 | 可观测性 | 零遥测证明 | 默认不向第三方发送语音、文本、按键；网络仅更新/模型下载 | 网络边界白名单 + 发布审计记录 | P0 默认基线 |

## 当前执行范围

本轮优先完成所有已复现或会阻断发布的 P0 项：构建恢复、窗口取消/关闭状态、历史时间戳无损往返、Windows 原子写入、更新下载不阻塞 UI、更新包完整性验证、无隐私发布、Rust 版本安装包和最近两版 Release 策略。M09、M11–M13、M21、M30–M31、M39–M43 等待用户在本矩阵中选择后再执行。

## 路线建议

推荐先选择“Windows 稳定交付线”：M01–M08、M10、M14–M20、M22–M26、M28–M29、M32–M38、M44。Android/平板不是给现有 Windows EXE 打补丁能得到的结果，必须进入平台适配架构（M39–M40）；第二识别引擎也应通过后端接口演进（M41），不能把 UI 和模型实现再次绑死。
