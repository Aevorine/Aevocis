## Aevocis 超语音 v1.3.4

这是 `v1.3.3` 的稳定性修复：

- **修复偶发崩溃**：主界面「历史记录」列表过去直接把存储层的内存列表作为绑定源，而新听写落库时会原地改动该列表且不发送变更通知，导致界面在某些布局/渲染时机抛出「ItemsControl 与它的项源不一致」而整个应用退出。现在每次渲染都绑定一份独立快照，任何时刻新增记录都不会再让界面失去同步。
- 单实例、崩溃后自恢复、桌面 Aevocis 图标与自动更新能力均保持不变。
