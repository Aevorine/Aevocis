## 超语音 Windows v1.2.0

一次专注"快、省、准"的识别引擎升级，本机实测（i5-1155G7）数据见下。

### 默认识别引擎换成「闪电」（SenseVoice int8）
- 6 秒中文音频识别耗时：186ms（原 Whisper small 2344ms），**快 12 倍**
- 识别峰值内存：341MB（原 1243MB），**省 73%**
- 中文输出：简体全对（原繁体漂移 + 错字）
- 对低电平/垃圾音频更不容易产生幻觉文本
- Whisper 仍可在设置页切换回去（多语种场景），模型改为按需下载，安装包因此缩小约 460MB

### 麦克风更聪明
- 蓝牙耳机只是"连着"但没戴时不再被错误抢作录音设备
- 没有手动固定麦克风时，内置麦克风与刚接入的设备会同时录音，停止时自动选出真正录到人声的那一路，谁选中的、为什么，日志里都能查到

### 其它
- 旧版 settings.json 无缝兼容，不会因为升级而丢失设置

---
本地识别，不联网、不上传录音或文字到任何服务器（下载 Whisper 模型时除外，会联网访问 Hugging Face）。
