## Aevocis 超语音 v1.3.3

这是 `v1.3.2` 的健壮性与发布卫生补丁：

- **崩溃后也能再次启动**：修复一个潜在的单实例缺陷——若上一次进程因异常而退出，被遗留的命名互斥锁会让下一次启动直接失败。现在会自动识别并接管该锁，Aevocis 总能正常启动。
- **exe 版本号修正**：文件「详细信息」里现在正确显示 `1.3.3`（此前固定显示 1.0.0，排查问题时容易误判实际运行的版本）。
- **单实例与图标保持不变**：重复点击只保留一个托盘图标、桌面图标继续使用 Aevocis 正式图标，自动更新链路继续工作。
