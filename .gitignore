# Build outputs
bin/
obj/

# Visual Studio
.vs/
*.user
*.suo

# Rider / ReSharper
.idea/
*.sln.iml

# Whisper model weights — too large for git history, shipped in the Release zip instead
OpenSuperWhisper.App/Models/*.bin

# Local packaging output (vpk pack / dotnet publish) — uploaded straight to GitHub Releases,
# never committed; huge binaries here would bloat history forever.
release-dist/

# OS cruft
Thumbs.db
.DS_Store
