# Build outputs
bin/
obj/

# Visual Studio
.vs/
*.user
*.suo

# Rider / ReSharper
.idea/
*.sln.iml

# Model weights — too large for git history, shipped inside the installer instead
# (see OpenSuperWhisper.App/Models/README.md for exact download sources).
OpenSuperWhisper.App/Models/*.bin
OpenSuperWhisper.App/Models/**/*.onnx

# Local packaging output (vpk pack / dotnet publish) — uploaded straight to GitHub Releases,
# never committed; huge binaries here would bloat history forever.
release-dist/

# OS cruft
Thumbs.db
.DS_Store
